@model List<ASPFinal.Models.Categories>

    <div class="single-blog-widget mb-50">
        <h3>categories</h3>
        <ul>
            @foreach (var item in Model)
            {
                <li><a href="#">@item.Name</a></li>
            }
        </ul>
    </div>

